import React from "react";
import clsx from "clsx";

const Todo = () => {
    return (
        <h2 className={clsx("text-2xl font-semibold capitalize")}>
            Pendências
        </h2>
      );
};

export default Todo;